# Local configuration. Copy to .env only if it does not exist. Never commit secrets.
GAME_ADDR=127.0.0.1:8080
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=classicfarm
MYSQL_USER=classicfarm
MYSQL_PASSWORD=
# With a blank password and no MYSQL_DSN the server runs in memory mode.
# MYSQL_DSN=