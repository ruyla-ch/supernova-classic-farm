# class-mid 邮箱验证记录

日期：2026-09-07

## 范围

- 新玩家欢迎邮件与账号、初始状态原子写入。
- 本人邮件查询、已读持久化、跨玩家隔离和非法编号。
- Vue 邮箱列表、未读计数以及胡萝卜文案。

## 已执行结果

- `go test ./... -count=1`：通过。
- `go vet ./...`：通过，无输出。
- `go test ./internal/game -run TestLiveMySQLRecovery -count=1 -v`：通过；欢迎邮件在新数据库连接中可读，已读状态成功写入。测试只使用唯一 `class_mid_*` 测试数据并在结束时清理。
- `npm.cmd test`：2 个测试通过，0 个失败。
- `npm.cmd run build`：通过，Vite 8.2.0 成功生成生产资源。

## 边界

- 沿用现有 `classicfarm` 数据库并自动创建 `class_mid_mails`，没有重建或清空数据库。
- 只有新注册玩家获得欢迎邮件，旧账号不补发。
- 邮箱没有附件、删除、过期、分页和主动推送。
- 本地浏览器 + MySQL 冒烟：通过。测试账号登录后显示恰好 4 块地，所有作物控件均为胡萝卜；首次打开邮箱显示 1 封未读欢迎邮件，点击后变为已读，关闭并再次打开邮箱仍显示已读。
