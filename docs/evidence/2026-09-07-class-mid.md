# class-mid 验证记录

日期：2026-09-07

## Go

执行：

```powershell
go test -count=1 -v ./...
go vet ./...
```

结果：`server/internal/game` 全部测试通过，`server/cmd/game` 无测试文件；vet 无错误。
覆盖注册/登录、未知字段拒绝、WebSocket 认证、购买、注销断连、密码哈希校验、完整农场循环、重复请求冲突、并发购买不超支、满仓失败不产生部分修改、MySQL commit/rollback。

设置仅用于测试的 `CLASS_MID_TEST_MYSQL_DSN` 后，`TestLiveMySQLRecovery` 在本机 MySQL 通过：测试创建独立 `class_mid_*` 临时账号，购买后使用新数据库连接读取，并用相同 request_id 验证不重复扣款，最后删除测试数据。没有修改 main 的旧表。

## Vue

执行：

```powershell
npm.cmd test
npm.cmd run build
npm.cmd audit --json
```

结果：未确认请求按玩家保存与恢复测试通过；`vue-tsc --noEmit` 与 Vite 生产构建通过；依赖审计为 0 个已知漏洞。

## 浏览器人工检查

启动 MySQL 模式单后端及 Vite 后，在 `http://localhost:5173` 完成：

1. 打开注册界面；
2. 注册测试账号并自动登录；
3. WebSocket AUTH 和初始快照成功；
4. 页面显示 10 金币、0 种子、1 肥料、4 块空地；
5. 购买 3 颗种子后显示 4 金币、3 种子，任务进度变为 3/3。

人工检查没有等待 100 秒完成整条种植生命周期；业务全循环由 Go 测试覆盖。测试服务随后已停止，当前 8080 与 5173 未监听。

## 结论边界

这些结果证明本机简化版的核心行为、事务与前端构建，不证明公网部署安全、长期稳定性、Qt 客户端兼容性或容量上限。
